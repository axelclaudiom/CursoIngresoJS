/* Melgar Claudio Axel Div H
Ejercicios If 05 corregir */
function mostrar()
{
	let edad;
	edad = txtIdEdad.value;
	edad = parseInt(edad);
	//if(!(edad >= 13 && edad<=17))
	//if(edad<13 || edad >17)
	if(edad >=13 || edad<=17)
	{
		alert("La persona es adolecente");
	}
}