/* Melgar Claudio Axel Div H
Ejercicios If 04*/
function mostrar()
{
	let edad;
	edad = txtIdEdad.value;
	edad = parseInt(edad);
	if(edad >= 13 && edad<=17)
	{
		alert("La persona es adolecente");
	}
}